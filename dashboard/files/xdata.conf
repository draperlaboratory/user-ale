input {
  file {
    codec => "json"
    path => "/var/log/xdata/xdata.log"
    start_position => beginning
  }
}

output {
  elasticsearch {
          index => xdata
    host => localhost
  }
  stdout { codec => rubydebug }
}

