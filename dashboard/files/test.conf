input {
  file {
    path => "/home/vagrant/xdata_log"
    start_position => beginning
    codec => "json"
  }


  unix {
    path => "/var/log/xdata-unix"
    codec => "json" 
  }
  
}

output {
  elasticsearch {
	  index => xdata
    host => localhost
  }
  stdout { codec => rubydebug }
}
