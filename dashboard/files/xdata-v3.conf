input {
  file {
    codec => "json"
    path => "/var/log/xdata/xdata-v3.log"
    start_position => beginning
  }
}

output {
  elasticsearch {
          index => xdatav3
    host => localhost
  }
  stdout { codec => rubydebug }
}

